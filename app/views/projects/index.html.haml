%h1 Projects
.projects
  - @projects.each do |project|
    = link_to project_path(project.slug), class: "project-card" do
      .thumbnail
        - if project.thumbnail.present?
          %img{src: "/uploads/projects/#{project.thumbnail}", alt: project.title}
      %h2.projectTitle= project.title
